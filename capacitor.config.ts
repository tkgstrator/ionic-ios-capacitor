import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "work.tkgstrator.salmonia3plusionic",
  appName: "Salmonia3+",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
